package com.chulgee.loadingimage;

import org.junit.Test;

/**
 * To work on unit tests, switch the Test Artifact in the Build Variants view.
 */
public class ExampleUnitTest {
    @Test
    public void test_memCache() throws Exception {
        /*byte[] arr = {1,2,3,4,5};

        MemCache mc = new MemCache(5);
        mc.put("1", BitmapFactory.decodeByteArray(arr, 0, arr.length));
        mc.put("2", BitmapFactory.decodeByteArray(arr, 0, arr.length));
        mc.put("3", BitmapFactory.decodeByteArray(arr, 0, arr.length));
        mc.put("4", BitmapFactory.decodeByteArray(arr, 0, arr.length));
        mc.put("5", BitmapFactory.decodeByteArray(arr, 0, arr.length));

        assertEquals(5, mc.getCurCacheSize());*/
    }

}